var Utility =require('./Utility');
var read = require('readline-sync');

 


var n=read.questionInt("enter the value of n ")


		     var  alphabet = "abc";
		     var elements = alphabet.substring(0, n);
		     //Utility.perm1(elements);
		    Utility. perm2(elements);
		